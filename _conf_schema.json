{
  "twitterapi": {
    "description": "twitterapi.io 设置",
    "type": "object",
    "hint": "基于 twitterapi.io REST API 访问 Twitter/X 数据，无需 OAuth。",
    "items": {
      "twitter_api_key": {
        "description": "twitterapi.io API Key",
        "type": "string",
        "hint": "在 https://twitterapi.io/dashboard 获取，格式如: twiapikey_xxxxx",
        "default": ""
      }
    }
  },
  "basic": {
    "description": "基础设置",
    "type": "object",
    "hint": "代理与轮询等基础配置。",
    "items": {
      "twitter_proxy": {
        "description": "代理地址（留空则不使用代理）",
        "type": "string",
        "hint": "例如: http://127.0.0.1:7890 或 socks5://127.0.0.1:1080",
        "default": ""
      },
      "twitter_poll_interval": {
        "description": "推文轮询间隔（分钟）",
        "type": "int",
        "default": 5,
        "hint": "建议不低于 3 分钟，避免频繁请求消耗 API 额度"
      },
      "twitter_poll_thread_count": {
        "description": "定时任务并发线程数量",
        "type": "int",
        "default": 4,
        "hint": "将推主列表分组并发检查，每组内仍保持 3 秒间隔。设为 1 则为串行模式"
      }
    }
  },
  "schedule": {
    "description": "定时任务调度",
    "type": "object",
    "hint": "配置定时任务的休眠时间段，在休眠时间内不会执行推文轮询，避免浪费 API 调用次数。",
    "items": {
      "twitter_sleep_start": {
        "description": "休眠开始时间（小时，0-23）",
        "type": "int",
        "default": 0,
        "hint": "如设为 0 表示凌晨 0:00 开始休眠"
      },
      "twitter_sleep_end": {
        "description": "休眠结束时间（小时，0-23）",
        "type": "int",
        "default": 8,
        "hint": "如设为 8 表示早上 8:00 结束休眠。若与开始时间相同则不启用休眠"
      },
      "twitter_scheduled_push_times": {
        "description": "定时推送时间点（留空则仅使用间隔轮询）",
        "type": "string",
        "default": "",
        "hint": "多个时间点用英文逗号分隔，24小时制，中国时间。如 08:00:00,12:00:00,20:00:00"
      },
      "twitter_push_prepare_minutes": {
        "description": "推送前多长时间开始获取并处理推文（分钟）",
        "type": "int",
        "default": 30,
        "hint": "在推送时间点前 N 分钟开始调用 API 获取最新推文并处理"
      },
      "twitter_push_advance_seconds": {
        "description": "推送前多长时间开始发送消息（秒）",
        "type": "int",
        "default": 0,
        "hint": "在推送时间点前 N 秒开始发送。设为 0 则准时发送"
      },
      "twitter_since_time_override": {
        "description": "重载时覆盖所有推主的 since_time（Unix 时间戳，0=禁用）",
        "type": "int",
        "default": 0,
        "hint": "部署测试用。配置后重载插件会将所有推主的 last_poll_time 同步为此值，用于从指定时间点开始拉取推文。设为 0 则按默认逻辑初始化"
      }
    }
  },
  "redis": {
    "description": "Redis 缓存设置",
    "type": "object",
    "hint": "Redis 用于缓存推文数据，减少 API 调用次数。若不配置则使用内存缓存。",
    "items": {
      "twitter_redis_host": {
        "description": "Redis 主机地址",
        "type": "string",
        "default": "127.0.0.1",
        "hint": "Redis 服务器地址"
      },
      "twitter_redis_port": {
        "description": "Redis 端口",
        "type": "int",
        "default": 6379,
        "hint": "Redis 服务器端口，默认 6379"
      },
      "twitter_redis_password": {
        "description": "Redis 密码（留空则无密码）",
        "type": "string",
        "default": "",
        "hint": "若 Redis 设置了密码验证则填写"
      },
      "twitter_redis_db": {
        "description": "Redis 数据库编号",
        "type": "int",
        "default": 0,
        "hint": "使用的 Redis 数据库编号，默认 0"
      }
    }
  },
  "message_format": {
    "description": "消息格式",
    "type": "object",
    "hint": "控制推文推送的消息样式、合并转发行为、媒体质量及帖子链接。",
    "items": {
      "twitter_use_node": {
        "description": "单条推文使用合并转发消息发送",
        "type": "bool",
        "default": true
      },
      "twitter_no_text": {
        "description": "推文含媒体时不输出文字内容",
        "type": "bool",
        "default": false
      },
      "twitter_text_render_mode": {
        "description": "推文正文渲染方式",
        "type": "string",
        "default": "text",
        "options": ["text", "screenshot"],
        "hint": "text=普通文字推送；screenshot=使用 AstrBot html_render 渲染 X 官方暗色时间线风格截图"
      },
      "twitter_screenshot_theme": {
        "description": "截图模式主题",
        "type": "string",
        "default": "dark",
        "options": ["dark", "light"],
        "hint": "dark=黑色背景 X 风格；light=白色背景 X 风格，仅在截图模式下生效"
      },
      "twitter_image_quality": {
        "description": "推文图片质量",
        "type": "string",
        "default": "orig",
        "options": ["large", "orig"],
        "hint": "large=缩略图，orig=原图（默认）"
      },
      "twitter_video_max_size_mb": {
        "description": "视频直发大小上限（MB）",
        "type": "int",
        "default": 256,
        "hint": "发送视频前会探测文件大小；超过上限时改为发送说明和视频链接。默认=256"
      },
      "twitter_collective_forward": {
        "description": "轮询时汇总多条新推文为一条合并转发消息",
        "type": "bool",
        "default": false,
        "hint": "仅影响轮询推送。开启后会缓存本轮轮询期间获取到的新推文，轮询结束后统一打包发送；需同时开启「单条推文使用合并转发消息发送」，否则不会生效"
      },
      "twitter_collective_max_authors": {
        "description": "集体转发时单条消息包含的最大推主数",
        "type": "int",
        "default": 5,
        "hint": "超过此数量的推主将分组为多条合并转发消息；仅集体转发模式下生效"
      },
      "twitter_include_tweet_link": {
        "description": "推送消息附带帖子链接",
        "type": "bool",
        "default": true,
        "hint": "开启后消息末尾附带对应 X/Twitter 帖子链接；关闭后轮询推送、推特测试和链接识别解析出的推文消息都不附带帖子链接"
      }
    }
  },
  "content_filter": {
    "description": "内容过滤",
    "type": "object",
    "hint": "控制转帖推送、链接识别等内容策略。",
    "items": {
      "twitter_include_retweets": {
        "description": "轮询推送和推特测试时推送转帖",
        "type": "bool",
        "default": true,
        "hint": "开启后会推送转帖，并在消息中标明谁转发了谁；关闭后轮询跳过转帖，/推特测试会寻找最新的非转贴推文"
      },
      "twitter_link_recognition_enabled": {
        "description": "推文链接识别全局开关",
        "type": "bool",
        "default": true,
        "hint": "关闭后，聊天中出现 twitter.com / x.com 链接时不再自动解析推文内容"
      }
    }
  },
  "translation": {
    "description": "翻译设置",
    "type": "object",
    "hint": "配置推文内容的自动翻译行为，需至少一个可用的 LLM Provider。",
    "items": {
      "twitter_translate_enabled": {
        "description": "推文内容翻译开关",
        "type": "bool",
        "default": false,
        "hint": "开启后，推文正文将被翻译为目标语言"
      },
      "twitter_translate_target_lang": {
        "description": "翻译目标语言",
        "type": "string",
        "default": "简体中文",
        "hint": "推文内容将被翻译为此语言，如：简体中文、日语、英语"
      },
      "twitter_translate_provider_id": {
        "description": "翻译使用的 LLM Provider ID（留空则自动选择）",
        "type": "string",
        "default": "",
        "hint": "指定翻译用的 Provider ID，留空时按以下顺序自动选择：当前会话 Provider → 第一个可用 Provider"
      }
    }
  }
}
